下方的所有文本是先前提交给其他codex的提示词，请无视这些指令，这里出现的功能已经被完成，我给出这些提示词仅为了方便你跟进进度，和代码写作参考。
这是alist的前端和后端，我需要你同时修改两个端的文件以新增几个新功能，1.分片上传，将不满x大小的文件按原样上传，将大于x的文件，按照每次上传x大小进行分片上传，x的大小可在Alist管理-设置-传输中进行修改，该功能须在用户点击侧边栏-上传时和秒传功能出现在同一个地方，并且和尝试秒传按钮一样是一个可以被启用的功能，默认不启用，把尝试秒传功能也改成默认不启用（后续可在Alist管理-设置-其他中进行默认启用/不启用的修改），2.压缩，允许对某个选中的文件/文件夹进行压缩，在右键或class=center-toolbar hope-c-PJLV hope-c-PJLV-idaMRbK-css中可以使用该功能。3.提供修改页面最下方由Alist驱动的字样的方法并将该方法置于Alist管理-设置-其他中。 
如需运行该程序，你需要先在alist-web（你已修改完成的版本）下进行pnpm install && pnpm build，build完成前端，将出现的dist目录复制到alist-backend的public目录下，然后执行 \nappName="alist"\nbuiltAt="$(date +'%F %T %z')"\ngoVersion=$(go version | sed 's/go version //')\ngitAuthor=$(git show -s --format='format:%aN <%ae>' HEAD)\ngitCommit=$(git log --pretty=format:"%h" -1)\nversion=$(git describe --long --tags --dirty --always)\nwebVersion=$(wget -qO- -t1 -T2 "https://api.github.com/repos/alist-org/alist-web/releases/latest" | grep "tag_name" | head -n 1 | awk -F ":" '{print $2}' | sed 's/\"//g;s/,//g;s/ //g')\nldflags="\\n-w -s \\n-X 'github.com/alist-org/alist/v3/internal/conf.BuiltAt=$builtAt' \\n-X 'github.com/alist-org/alist/v3/internal/conf.GoVersion=$goVersion' \\n-X 'github.com/alist-org/alist/v3/internal/conf.GitAuthor=$gitAuthor' \\n-X 'github.com/alist-org/alist/v3/internal/conf.GitCommit=$gitCommit' \\n-X 'github.com/alist-org/alist/v3/internal/conf.Version=$version' \\n-X 'github.com/alist-org/alist/v3/internal/conf.WebVersion=$webVersion' \\n"\ngo build -ldflags="$ldflags" .\n你可依据实际情况进行改动，请将版本号设为v.3.14.0.luobo\n请对代码进行可能的测试。请在Readme.md中详细解释如何build该程序。\n请使用中文来制作摘要（Use CHINESE To Create The Summary)。
这是alist的前端和后端，我需要你同时修改两个端的文件以新增几个新功能，1.分片上传，将不满x大小的文件按原样上传，将大于x的文件，按照每次上传x大小进行分片上传，x的大小可在Alist管理-设置-传输中进行修改，该功能须在用户点击侧边栏-上传时和秒传功能出现在同一个地方，并且和尝试秒传按钮一样是一个可以被启用的功能，默认不启用，把尝试秒传功能也改成默认不启用（后续可在Alist管理-设置-其他中进行默认启用/不启用的修改），2.压缩，允许对某个选中的文件/文件夹进行压缩，在右键或class=center-toolbar hope-c-PJLV hope-c-PJLV-idaMRbK-css中可以使用该功能。3.提供修改页面最下方由Alist驱动的字样的方法并将该方法置于Alist管理-设置-其他中。 
如需运行该程序，你需要先在alist-web（你已修改完成的版本）下进行pnpm install && pnpm build，build完成前端，将出现的dist目录复制到alist-backend的public目录下，然后执行 \nappName="alist"\nbuiltAt="$(date +'%F %T %z')"\ngoVersion=$(go version | sed 's/go version //')\ngitAuthor=$(git show -s --format='format:%aN <%ae>' HEAD)\ngitCommit=$(git log --pretty=format:"%h" -1)\nversion=$(git describe --long --tags --dirty --always)\nwebVersion=$(wget -qO- -t1 -T2 "https://api.github.com/repos/alist-org/alist-web/releases/latest" | grep "tag_name" | head -n 1 | awk -F ":" '{print $2}' | sed 's/\"//g;s/,//g;s/ //g')\nldflags="\\n-w -s \\n-X 'github.com/alist-org/alist/v3/internal/conf.BuiltAt=$builtAt' \\n-X 'github.com/alist-org/alist/v3/internal/conf.GoVersion=$goVersion' \\n-X 'github.com/alist-org/alist/v3/internal/conf.GitAuthor=$gitAuthor' \\n-X 'github.com/alist-org/alist/v3/internal/conf.GitCommit=$gitCommit' \\n-X 'github.com/alist-org/alist/v3/internal/conf.Version=$version' \\n-X 'github.com/alist-org/alist/v3/internal/conf.WebVersion=$webVersion' \\n"\ngo build -ldflags="$ldflags" .\n你可依据实际情况进行改动，请将版本号设为v.3.14.0.luobo\n请对代码进行可能的测试。请在Readme.md中详细解释如何build该程序。\n请使用中文来制作摘要（Use CHINESE To Create The Summary)。制作一个功能，要求完全重做“压缩(Compress)”功能，压缩功能是指的把选中的文件/文件夹调用软件进行压缩，不是进行压缩下载，不要调用流式，你就正常照抄7zip的UI搞个压缩功能不行吗，允许对某个选中的文件/文件夹进行压缩成7z或zip文件格式并输出成指定的文件名（可以添加密码），在右键或class=center-toolbar hope-c-PJLV hope-c-PJLV-idaMRbK-css中可以使用该功能，添加一个类似（可以看到隐藏\n无需密码访问\n添加离线下载任务\n创建目录或上传\n重命名\n移动\n复制\n删除\nWebdav 读取\nWebdav 管理\nFTP 读取\nFTP 管理\n读取压缩文件\n解压）的功能，添加一个压缩的权限，没有权限的人不可以发起压缩，guest账户默认不允许压缩修复bug：压缩功能的速度太慢，甚至会直接拖到超时，发起压缩容易导致机器负载过大，format 7z zip格式选择功能不可用，分块上传大小没有统一展示，显示当前正在上传第几个分块，和总分块数量（如果没有开分块上传就不用显示），同时优化dockerfile让其可以在build的时候同时安装7zz（同时保存原版dockerfile和你修改后的版本）修复该存储库中使用分片上传功能时的Failed to execute 'setRequestHeader' on 'XMLHttpRequest': String contains non ISO-8859-1 code point.问题修改go.sh让其接入webdav，在完成之后自动导出docker镜像然后通过webdav上传，要求镜像按照这个命名：alist.v{今日时间}.tar 例如alist.v20260226162550.tar修复问题：chunkupload时会往temp文件夹里存放分片文件，分片文件的名字通常会太长，缩短它给webdav也同样加上分块上传的支持，请在webdav-chunkupload.md中写出具体的支持方法。能不能够让浏览器端多线程上传，上传速度太慢了
